import styled from "@emotion/styled";
import { Button } from "@mui/material";

export const ToTopButtonLayout = styled.div`
  display: flex;
  padding: 1rem;
`;

export const ToTopButton = styled(Button)`
  flex-grow: 1;
`;
